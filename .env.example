# Copy to .env and fill in from your Supabase project's Settings → API.
# The anon/publishable key is safe to expose client-side — it's the row-level
# security policies on each table that actually keep data private.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
